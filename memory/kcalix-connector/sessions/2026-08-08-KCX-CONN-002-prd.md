# Sessão 2026-08-08 — KCX-CONN-002 PRD do piloto

Issue: `KCX-CONN-002`
Modo: Especificar
Branch: `codex/kcalix-connector`
Resultado: spec e PRD aprovados tecnicamente; G1 — Valor fechado; código continua bloqueado.

## Entrega

- Criada a spec `specs/KCX-CONN-002.md`, estado `APPROVED`.
- Criado o PRD `PRD.md`, versão `kcx-connector-prd/1`, estado `APPROVED`.
- Mantidos exatamente três casos: cardio sem redigitação, musculação enriquecida e body fat BIA.
- Definido baseline manual de 7 dias sem valores de saúde.
- Definido piloto de 14–28 dias com métricas de automação, correção, duplicidade,
  confiabilidade, fricção, cobertura e valor percebido.
- Definidos critérios `GO`, `ADJUST` e `NO-GO` e o proprietário como responsável pela decisão
  de continuidade.
- Aprovada a política `kcx-hr-zones/1`: zonas somente com FC máxima informada/validada; sem
  estimativa silenciosa por idade e sem linguagem médica.

## Consistência com a KCX-CONN-001

- Kcalix continua fonte de exercícios, séries, repetições, cargas, alimentação e água.
- Watch apenas complementa resumos confirmados e nunca sobrescreve o treino estruturado.
- Mesma data não prova match; parâmetros finais aguardam `KCX-CONN-007`.
- `TotalCaloriesBurnedRecord` não é energia ativa e não altera saldo alimentar.
- Body fat BIA, JP7 e manual permanecem métodos distintos.
- Passos, sono, FC de repouso, background, Play Store, Coach automático e uso médico continuam
  fora do MVP.

## Validação

- Os cinco critérios da Issue foram confrontados com a spec e o PRD e estão cobertos.
- O PRD contém exatamente três casos prioritários.
- Baseline, fórmulas, denominadores mínimos, período e limiares estão explícitos.
- Ausência é diferenciada de zero; poucos eventos produzem resultado `inconclusivo`.
- Métricas não exigem valor bruto de saúde, token, ID externo ou screenshot.
- Nenhum arquivo em `connector/android/`, `src/` ou `supabase/` foi alterado pela Issue.

Verificações do Start Session:

- `npm run build`: passou; apenas avisos preexistentes de chunk e `caniuse-lite`.
- Contrato `kcx-health-observation/1`: duas fixtures aceitas, versão inválida rejeitada,
  privacidade estrutural e seis permissões confirmadas.
- `:app:testDebugUnitTest`: passou.
- `:app:assembleDebug`: não concluiu por artefato `.dex` gerado no diretório OneDrive marcado
  como reparse point (`not a regular file`). O código Kotlin não falhou; o APK preexistente
  continua no workspace. Nenhuma limpeza foi feita sem autorização. Esse empacotamento não é
  critério da Issue documental, mas deve ser reconstruído antes de uma entrega Android.

## Estado final

- `KCX-CONN-002`: `DONE`.
- Spec: `APPROVED`.
- PRD: `APPROVED`.
- Gate: G1 — Valor fechado.
- Próxima Issue: `KCX-CONN-003`, `READY`.
- Próximo modo: Especificar.
- Resultado esperado: ADR que ratifica ou rejeita Kotlin/Compose para o piloto e registra as
  condições de eventual unificação via Capacitor.
- Incluído na próxima Issue: base Android existente, compatibilidade Health Connect, debugging,
  autenticação futura, distribuição e custo de migração.
- Não incluído: permissões, leitura real, Supabase, sync, APK release, Play Store ou merge.
- Primeira ação exata: criar `specs/KCX-CONN-003.md` e a ADR após inspecionar a configuração e
  a estrutura atuais de `connector/android/`.

Nenhum commit, push, deploy, migration remota ou APK foi publicado nesta sessão.

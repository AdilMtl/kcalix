// App.tsx — roteador principal do Kcalix
// Fase 0: placeholder. Fase 1 adiciona autenticação e navegação real.

export default function App() {
  return (
    <div className="flex min-h-dvh items-center justify-center" style={{ background: 'var(--bg)' }}>
      <div className="text-center">
        <h1 className="text-2xl font-bold" style={{ color: 'var(--accent)' }}>Kcalix</h1>
        <p className="mt-2 text-sm" style={{ color: 'var(--text2)' }}>Setup concluído. Fase 1 em breve.</p>
      </div>
    </div>
  )
}
